# Keycloak Test Framework configuration used when running integration tests from the IDE.
# Maven (failsafe) passes the same settings as system properties, which take precedence.
KC_TEST_SERVER=distribution
KC_TEST_SERVER_HOT_DEPLOY=true
